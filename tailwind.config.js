/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./index.html", // Scans your HTML files
      "./src/**/*.{js,jsx,ts,tsx}", // Scans all JS, JSX, TS, and TSX files within the src folder
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }
  